<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/navigation_item_layout"
    android:layout_width="match_parent"
    android:layout_height="@dimen/navigation_menu_header_height"
    android:background="@drawable/navigation_image"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/navigation_item_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingLeft="@dimen/navigation_menu_item_horizontal_padding"
        android:src="@drawable/ic_mood_white_48dp" />

    <TextView
        android:id="@+id/navigation_item_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingLeft="@dimen/navigation_menu_item_horizontal_padding"
        android:paddingRight="@dimen/navigation_menu_item_horizontal_padding"
        android:text="@string/app_name"
        android:textColor="@color/primary_material_light"
        android:textSize="20sp" />

</LinearLayout>